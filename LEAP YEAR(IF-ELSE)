LEAP YEAR (IF-ELSE):

#include <stdio.h>

int main() {
    int a;
    scanf("%d",&a);
    if((a%4==0)&&(a%400==0)||(a%100!=0)){
        printf("%d is a Leap year",a);
       }
    else{
        printf("%d is Not a Leap Year",a);
    }

    return 0;
}
